  @using DataLayer.Contract;
  @using System.Reflection;
  @using UILayer.Miscellaneous;
  @model IContract  
  
    @Html.Label(ViewData["langugeValue"].ToString())
    @{var propInfo=((PropertyInfo)ViewData["PropertyInfo"]);
 <input id='@(propInfo.Name )' name='@(propInfo.Name )' type="password"    value='@(propInfo.GetValue(Model,null))'  onblur= '@( ViewData["validateMethod"].ToString())' />                                                    
    <br  />

 }
